<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="it.polimi"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:label="@string/app_name">
        <activity android:name="Start"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.student.Students" android:label="All students"/>
        <activity android:name=".screens.student.NewStudent" android:label="Add student"/>
        <activity android:name=".screens.student.ViewStudent" android:label="View student"/>

        <activity android:name=".screens.course.Courses" android:label="All courses"/>
        <activity android:name=".screens.course.NewCourse" android:label="Add course"/>
        <activity android:name=".screens.course.ViewCourse" android:label="View course"/>

        <activity android:name=".screens.teacher.Teachers" android:label="All teachers"/>
        <activity android:name=".screens.teacher.NewTeacher" android:label="Add teacher"/>
        <activity android:name=".screens.teacher.ViewTeacher" android:label="View teacher"/>
    </application>
</manifest> 
