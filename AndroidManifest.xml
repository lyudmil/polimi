<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="it.polimi"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:label="@string/app_name">
        <activity android:name="Start"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Students" android:label="All students"/>
        <activity android:name="NewStudent" android:label="Add student"/>
        <activity android:name="ViewStudent" android:label="View student"/>

        <activity android:name="Courses" android:label="All courses"/>
        <activity android:name="NewCourse" android:label="Add course"/>
        <activity android:name="ViewCourse" android:label="View course"/>
    </application>
</manifest> 
